###############################################################################
# TenRusl Payment Webhook Simulator — Example Environment
# Salin file ini menjadi .env lalu sesuaikan nilainya
###############################################################################

APP_NAME="TenRusl Payment Webhook Sim"
APP_ENV=local
APP_KEY=base64:jNx1QaF9oWeFaTb+LYeFQoAi2V2zZPAxZUi4V278XwU=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database
ANALYTICS_ENDPOINT=

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4173

###############################################################################
# DATABASE — PILIH SATU
###############################################################################

# --- Opsi default (disarankan untuk lokal): SQLite (tanpa service DB)
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

# --- Opsi alternatif: MySQL (Laragon/Docker), uncomment & isi
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=tenrusl_webhook
# DB_USERNAME=root
# DB_PASSWORD=
# (Jika pakai Docker compose dengan service "mysql", host= mysql , port= 3306)

###############################################################################
# SESSION / CACHE / QUEUE — aman di lokal tanpa driver DB
###############################################################################

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

QUEUE_CONNECTION=database
CACHE_STORE=file
# CACHE_PREFIX=

###############################################################################
# REDIS (opsional)
###############################################################################

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

###############################################################################
# MAIL — log driver untuk dev
###############################################################################

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="noreply@tenrusl.local"
MAIL_FROM_NAME="${APP_NAME}"

###############################################################################
# AWS (opsional)
###############################################################################

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

###############################################################################
# Frontend (Vite)
###############################################################################

VITE_APP_NAME="${APP_NAME}"

###############################################################################
# TenRusl-specific
###############################################################################
# Dipakai oleh config/tenrusl.php untuk:
# - retry webhook & scheduler
# - idempotensi
# - dedup webhook
# - signature timestamp leeway

# Jumlah maksimal percobaan retry webhook
TENRUSL_MAX_RETRY_ATTEMPTS=5

# Backoff parameters (ms) untuk retry (dipakai RetryBackoff via RetryWebhookCommand)
TENRUSL_RETRY_BASE_MS=500
TENRUSL_RETRY_CAP_MS=30000

# TTL idempotensi legacy (fallback). Konfigurasi utama ada di *_IDEMPOTENCY_TTL_SECONDS.
TENRUSL_IDEMPOTENCY_TTL=3600

# TTL idempotensi utama (prefer TENRUSL_IDEMPOTENCY_TTL_SECONDS; fallback IDEMPOTENCY_TTL_SECONDS)
TENRUSL_IDEMPOTENCY_TTL_SECONDS=7200
IDEMPOTENCY_TTL_SECONDS=7200

# Storage idempotency (default cache). "database" hanya dipakai jika implementasi DB idempotency sudah ada.
IDEMPOTENCY_STORAGE=cache

# Lock idempotency (detik) untuk cegah race pada create payment
IDEMPOTENCY_LOCK_SECONDS=30

# Pengaturan scheduler retry webhook (dibaca via tenrusl.scheduler_*)
TENRUSL_SCHEDULER_PROVIDER=
TENRUSL_SCHEDULER_BACKOFF_MODE=full
TENRUSL_SCHEDULER_LIMIT=200

# TTL dedup webhook (prefer TENRUSL_WEBHOOK_DEDUP_TTL_SECONDS; fallback WEBHOOK_DEDUP_TTL_SECONDS)
TENRUSL_WEBHOOK_DEDUP_TTL_SECONDS=86400
WEBHOOK_DEDUP_TTL_SECONDS=86400

# Signature timestamp leeway (detik) — untuk provider yang pakai header timestamp
TENRUSL_SIG_TS_LEEWAY=300
SIG_TS_LEEWAY=300

###############################################################################
# Provider demo (untuk verifikasi signature/token)
###############################################################################

MOCK_SECRET=changeme
XENDIT_CALLBACK_TOKEN=changeme
MIDTRANS_SERVER_KEY=changeme

###############################################################################
# Provider Tambahan (opsional): isi yang dibutuhkan saja
###############################################################################

STRIPE_WEBHOOK_SECRET=

PAYPAL_ENV=sandbox
PAYPAL_WEBHOOK_ID=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

PADDLE_SIGNING_SECRET=
# PADDLE_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"

LS_WEBHOOK_SECRET=
AIRWALLEX_WEBHOOK_SECRET=
TRIPAY_PRIVATE_KEY=

DOKU_CLIENT_ID=
DOKU_SECRET_KEY=
DOKU_REQUEST_TARGET=/orders/v1/status/INV-123

DANA_PUBLIC_KEY=

OY_CALLBACK_SECRET=
OY_IP_WHITELIST=

PAYONEER_SHARED_SECRET=
PAYONEER_MERCHANT_ID=

SKRILL_MERCHANT_ID=
SKRILL_EMAIL=
SKRILL_MD5_SECRET=

AMZN_BWP_PUBLIC_KEY=
