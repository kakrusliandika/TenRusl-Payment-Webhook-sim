# Redocly CLI config (root project)
apis:
  tenrusl:
    root: docs/openapi.yaml

extends:
  - recommended

rules:
  # Biar lebih rapih dan konsisten
  operation-operationId: error
  no-unused-components: warn
