name: PHP CI (disabled)

on: {}

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled (merged into ci.yml)
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is intentionally disabled
        run: echo "Merged into .github/workflows/ci.yml. Safe to delete after cleanup."
